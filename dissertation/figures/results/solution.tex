\begin{tikzpicture}
\begin{axis}[
  scale=1.15,
  grid=major,
  colorbar,
  colormap/viridis,
  xlabel=$x$,
  ylabel=$y$,
  zlabel={$u_\mathrm{sol}(x,y)$}
  ]
\addplot3[
  surf,
  domain=-1:1,
  y domain=-1:1,
  restrict expr to domain={(x>0)&&(y<0)}{0:0},
  samples=21
  ]{pow(x*x+y*y,0.33)*sin(0.66*(atan2(y,-x)+90)))};
\end{axis}
\end{tikzpicture}